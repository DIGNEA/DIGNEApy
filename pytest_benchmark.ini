# Config for pytest benchmarks. Pass this file to pytest with the -c option.

[pytest]
python_files =  benchmark_*.py
python_functions = benchmark_*
addopts = -v --cov-report term-missing --cov=digneapy --benchmark-sort=name --benchmark-name=long
markers = style